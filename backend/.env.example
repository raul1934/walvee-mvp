# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=3000
API_VERSION=v1

# ===========================================
# DATABASE CONFIGURATION (MySQL)
# ===========================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=walvee
DB_USER=root
DB_PASSWORD=your_mysql_password_here

# ===========================================
# JWT CONFIGURATION
# ===========================================
# Generate secure secrets using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_super_secret_refresh_key_here_change_in_production
JWT_REFRESH_EXPIRES_IN=30d

# ===========================================
# GOOGLE OAUTH 2.0 CONFIGURATION
# ===========================================
# Get credentials from: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URI: http://localhost:3000/v1/auth/google/callback
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_CALLBACK_URL=http://localhost:3000/v1/auth/google/callback

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
# Frontend URL for OAuth redirects and CORS
# Development: http://localhost:5173
# Production: https://your-domain.com
FRONTEND_URL=http://localhost:5173

# ===========================================
# SESSION CONFIGURATION
# ===========================================
# Generate secure secret using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SESSION_SECRET=your_session_secret_here_change_in_production

# ===========================================
# FILE UPLOAD CONFIGURATION
# ===========================================
# Local storage directory (relative to backend root)
UPLOAD_DIR=uploads
# Max file size in bytes (default: 5MB)
MAX_FILE_SIZE=5242880

# ===========================================
# OPTIONAL: EXTERNAL SERVICES
# ===========================================
# Google Maps API (for place details, geocoding, etc.)
# Get key from: https://console.cloud.google.com/google/maps-apis
# GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# OpenAI API (for AI-powered features like trip recommendations)
# Get key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here

# Cloud Storage (for production file uploads)
# AWS S3
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name

# OR Google Cloud Storage
# GCS_PROJECT_ID=your_gcs_project_id
# GCS_BUCKET=your_gcs_bucket_name
# GCS_KEYFILE=path/to/keyfile.json

# ===========================================
# PRODUCTION SETTINGS
# ===========================================
# For production deployment, ensure:
# 1. Set NODE_ENV=production
# 2. Use strong, unique secrets for JWT and SESSION
# 3. Set FRONTEND_URL to your production domain
# 4. Use HTTPS for GOOGLE_CALLBACK_URL
# 5. Configure cloud storage instead of local uploads
# 6. Set up proper database with backups
# 7. Enable SSL/TLS for database connections
